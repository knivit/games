package com.tsoft.dune2.gui.widget;

public class Flags {

    public boolean requiresClick;                             /*!< Requires click. */
    public boolean notused1;
    public boolean clickAsHover;                              /*!< Click as hover. */
    public boolean invisible;                                 /*!< Widget is invisible. */
    public boolean greyWhenInvisible;                         /*!< Make the widget grey out when made invisible, instead of making it invisible. */
    public boolean noClickCascade;                            /*!< Don't cascade the click event to any other widgets. */
    public boolean loseSelect;                                /*!< Lose select when leave. */
    public boolean notused2;
    public int buttonFilterLeft;                              /*!< Left button filter. */
    public int buttonFilterRight;                             /*!< Right button filter. */
}
